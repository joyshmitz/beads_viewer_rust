# bv (beads viewer) local config and caches
.bv/
target/
a.out
.ruff_cache/

# SQLite runtime files
storage.sqlite3
storage.sqlite3-*
*.sqlite3.bak

# Go reference binary (built during conformance tests)
tests/conformance/go_reference/bvr

# Core dumps
core.*

# Corrupt/backup database files
*.corrupt
*.corrupt_bak
*.corrupt-*
*.pre_repair_*.bak
*.db.new

# Alternative build targets
target-*/
.env
.claude/settings.local.json

# ACFS ephemeral patterns (auto-generated agent artifacts)
cline.mcp.json
codex.mcp.json
cursor.mcp.json
gemini.mcp.json
opencode.json
windsurf.mcp.json
*.mcp.json.*.bak
.opencode.json.*.bak
.windsurf.mcp.json.*.bak
.agent-mail-project-id
.agent-mail.yaml
*.sqlite3.corrupt*
storage.sqlite3.corrupt-*
storage.sqlite3-shm.corrupt-*
storage.sqlite3-wal.corrupt-*
.beads/*.corrupt*
.beads/*.pre_repair_*
.beads/*.repair_candidate_*
.beads/*.bak-corrupt

# Snapshot test review artifacts (regenerated by insta)
*.snap.new
