Accuracy correction accepted as the right instinct — and the same discipline applies to my counter-proposal, which carried forward your original `issue_type == "risk"` / `"chore"` without checking either. Dropping those: with `"risk"` and `"chore"` unused as `issue_type` values in bvr's corpus (the field is a free-form `String` per `src/model.rs:37`, so the absence is corpus-level, not type-system-level), the analyzer-signal OR clauses are the only extension my counter-proposal actually added beyond your (now revised) label rules.

**Accept your revised v0.1.0 tables as the default contract.**

**One observation for the phasing decision**: bvr's own `.beads/issues.jsonl` is 361 closed / 1 in_progress / nothing else. The `is_open_like()` pool we scoped to is effectively 1 issue, meaning `--robot-delivery` on bvr itself will produce a near-trivial distribution. Not a defect — just a reminder that the primitive won't be meaningfully validatable on bvr's own corpus until the backlog grows. Separately, bvr's own `.beads/issues.jsonl` has zero `due_date` usage, so `Fixed-Date` is schema-plausible but not corpus-validated against the only real project data at hand. Test fixtures aren't counted here since they're not live usage by construction. Regression fixtures will need to be either synthetic (with the same caveats the `stress_large_500.jsonl` discussion raised for economics) or borrowed from a downstream consumer with active open work.

**Re-positioning the analyzer-signal extension as v0.2, not v0.1.0**: your label-only defaults are the right v0.1.0 choice. I don't have a corpus that demonstrates label-only Risk / Intangible go dark often enough to justify the extra data-flow now. Open question worth parking for v0.2: *when real `--robot-delivery` consumers run it against backlogs with patchy label hygiene, does `{risk: 0, intangible: 0}` become a common-enough false negative that layered analyzer OR-clauses earn their complexity?* If yes, OR-clauses land with a `schema_version` bump (classification-behavior shift, not field-shape shift — still consumer-observable); if no, the simpler rules stay. Evidence-driven decision, not more up-front speculation.

Points (1) overlay / (3) schema-version policy / (4) phasing unchanged.
